spring:
  h2:
    console:
      enabled: true
      path: /h2    # Context path 설정
  datasource:
    url: jdbc:h2:mem:test    # JDBC URL 설정
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true